fastapi==0.115.0
uvicorn[standard]==0.30.6
playwright==1.47.0
# playwright-stealth imports pkg_resources at module load time but doesn't
# declare setuptools as a dependency itself - not present by default in
# this base image, so it must be listed explicitly or the import crashes
# the whole app on startup.
setuptools
playwright-stealth==1.0.6
